import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#258BB6",
      light: "#37abd6",
      dark: "#185a81",
    },
  },
});

export default theme;
